name: .NET

on:
  workflow_dispatch:
  

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Dump github context
      run: | 
        echo github context
        echo ${{ format( github.event.repository.git_tags_url, 'latest') }}
